%h3 Obras

- @artworks.each do |artwork|
  %p
    %strong
      %a{:href=>"/artworks/#{artwork['id']}", :title=> artwork['title']}
        #{artwork['title']}
    = artwork['year'] unless artwork['year'] == 0 || artwork['year'].nil?
    %br
    T&eacute;cnica: #{artwork['technique']}
    