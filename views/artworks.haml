%h3 Obras

- @artworks.each do |artwork|
  %p
    %strong
      #{artwork['title']}
    = artwork['year'] unless artwork['year'] == 0 || artwork['year'].nil?
    %br
    Obras: #{artwork['technique']}
    